import React from 'react';  //core foundational lib
import ReactDOM from 'react-dom/client';  //to manipulate web dom
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


